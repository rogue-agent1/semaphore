# semaphore

Counting semaphore implementation

Single-file, zero-dependency Python CLI tool.

## Usage

```bash
python semaphore.py --help
```

## Test

```bash
python -c "import semaphore; semaphore.test()"
```
